<html>
  <% include('../../partials/header.ejs') %>

  <body>
    <% include('../../partials/navBar.ejs') %> <%
    include('../../partials/sideBarAdmin.ejs') %>

    <div class="mainContainer">
      <section class="container">
        <h1 class="title is-1">Responder retroalimentacion</h1>

        <form class="preguntaContainer" method="POST" action="/actual/enviado">
          <% let cantCualitativas = 0; %> <% for (let i = 0; i <
          preguntas.length; i++) { %> <% if(preguntas[i].contenido != '¿Que
          podemos mejorar?') { %> <% if (preguntas[i].tipoPregunta ==
          'Cualitativa') { %>
          <div
            class="box is-flex is-flex-direction-column is-align-self-center"
          >
            <label class="label" for="respuestaCualitativa"
              ><%= preguntas[i].contenido %></label
            >
            <input
              class="input m-3 is-justify-content-center is-align-self-center"
              type="text"
              id="respuestaCualitativa"
              name="<%= preguntas[i].idPregunta %>"
              placeholder="Respuesta"
              required
            />
          </div>
          <% } else { %>
          <div class="box is-flex is-flex-direction-column is-vcentered">
            <label class="label" for="respuestaCuantitativa"
              ><%= preguntas[i].contenido %></label
            >
            <% include('../../partials/preguntaCuantitativa.ejs',
            {name:preguntas[i].idPregunta }) %>
          </div>
          <% } %> <% } else { %>
          <div class="box is-flex is-flex-direction-column is-vcentered">
            <label class="label" for="respuestaCuantitativa"
              ><%= preguntas[i].contenido %></label
            >
            <input
              class="input m-3 is-justify-content-center is-align-self-center"
              type="text"
              id="respuestaCualitativa"
              name="8"
              placeholder="Respuesta"
              required
            />
          </div>
          <% } %> <% } %>

          <div class="is-flex is-justify-content-center">
            <input
              class="button is-danger is-rounded is-medium mb-6"
              type="submit"
              value="Enviar"
            />
          </div>
        </form>
      </section>
    </div>
  </body>
</html>

<style>
  ::-webkit-scrollbar {
    width: 0;
    height: 0;
    background-color: transparent;
  }
  .box {
    background-color: rgb(38, 43, 43) !important;
    border: 1px solid #181a1b !important;
  }

  .title {
    color: #e3e1dd !important;
  }

  .label {
    color: #e3e1dd !important;
    margin-left: 4rem;
  }

  body {
    background-color: #181a1b;
    color: #e3e1dd !important;
  }

  section {
    background-color: #181a1b;
  }

  .accionableText {
    margin-left: 4rem;
  }

  .mainContainer {
    background-color: #181a1b;
    width: 85%;
    margin-left: 15%;
    padding-top: 6% !important;
    height: 92%;

    padding: 0px;
  }
  .preguntaContainer {
    margin-top: 5%;
  }
</style>
